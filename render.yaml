services:
  - type: web
    name: jan-ai-chatbot
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: TELEGRAM_TOKEN
        fromSecret: true
      - key: FB_VERIFY_TOKEN
        generateValue: true
